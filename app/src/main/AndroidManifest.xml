<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.leado">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".buttonNavigationActivites.Fragment2"></activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".AwarenessRoom" />
        <activity android:name=".CongratsScreen" />
        <activity android:name=".couseActivities.VideoActivity" />
        <activity android:name=".startActivities.Fragment1_">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>